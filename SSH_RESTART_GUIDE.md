# 🔄 Инструкция по перезапуску проекта через SSH

## 📋 Быстрый перезапуск

### Вариант 1: Использование готового скрипта (PM2)

```bash
# Подключение к серверу
ssh username@your-server-ip

# Переход в директорию проекта
cd /path/to/digital-hustlas-cursor

# Простой перезапуск
./restart.sh
```

### Вариант 2: Ручной перезапуск через PM2

```bash
# Подключение к серверу
ssh username@your-server-ip

# Переход в директорию проекта
cd /path/to/digital-hustlas-cursor

# Вариант А: Перезапуск всех процессов PM2
pm2 restart all

# Вариант Б: Перезапуск конкретных процессов
pm2 restart digital-hustlas-api
pm2 restart digital-hustlas-web

# Вариант В: Полная перезагрузка (остановка + запуск)
pm2 stop all
pm2 start all

# Вариант Г: С пересборкой проекта
npm run build
pm2 restart all
```

### Вариант 3: Использование PM2 ecosystem (рекомендуется)

```bash
# Подключение к серверу
ssh username@your-server-ip

# Переход в директорию проекта
cd /path/to/digital-hustlas-cursor

# Пересборка проекта
npm run build

# Перезапуск через ecosystem.config.cjs
pm2 restart ecosystem.config.cjs

# Или остановка и запуск
pm2 stop ecosystem.config.cjs
pm2 start ecosystem.config.cjs
```

### Вариант 4: Docker Compose

```bash
# Подключение к серверу
ssh username@your-server-ip

# Переход в директорию проекта
cd /path/to/digital-hustlas-cursor

# Перезапуск контейнеров
docker-compose restart

# Или с пересборкой
docker-compose down
docker-compose up -d

# Или с полной пересборкой образа
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

## 🔍 Проверка статуса

```bash
# Проверка процессов PM2
pm2 status
pm2 logs

# Проверка Docker контейнеров
docker ps
docker-compose ps

# Проверка портов
netstat -tlnp | grep -E '3000|3001'
# или
ss -tlnp | grep -E '3000|3001'

# Проверка API
curl http://localhost:3001/api/health
```

## 🛠️ Полный перезапуск с обновлением кода

```bash
# 1. Подключение к серверу
ssh username@your-server-ip

# 2. Переход в директорию проекта
cd /path/to/digital-hustlas-cursor

# 3. Получение последних изменений из git
git pull origin main

# 4. Установка зависимостей (если нужно)
npm install

# 5. Пересборка проекта
npm run build

# 6. Перезапуск (выберите один из вариантов выше)
./restart.sh
# или
pm2 restart all
# или
docker-compose restart
```

## 📝 Полезные команды PM2

```bash
# Статус всех процессов
pm2 status

# Логи всех процессов
pm2 logs

# Логи конкретного процесса
pm2 logs digital-hustlas-api
pm2 logs digital-hustlas-web

# Мониторинг в реальном времени
pm2 monit

# Сохранение текущей конфигурации
pm2 save

# Удаление всех процессов
pm2 delete all

# Перезапуск с очисткой логов
pm2 flush
pm2 restart all
```

## 📝 Полезные команды Docker

```bash
# Просмотр логов
docker-compose logs -f
docker-compose logs -f digital-hustlas

# Остановка всех контейнеров
docker-compose down

# Запуск в фоновом режиме
docker-compose up -d

# Пересборка без кэша
docker-compose build --no-cache

# Просмотр использования ресурсов
docker stats
```

## ⚠️ Важные замечания

1. **База данных**: Убедитесь, что база данных `/var/www/vhosts/194.32.140.220.nip.io/ProdB/database.db` не перезаписывается
2. **Логи**: Проверяйте логи при проблемах: `pm2 logs` или `docker-compose logs`
3. **Порты**: Убедитесь, что порты 3000 и 3001 свободны
4. **Права доступа**: Убедитесь, что скрипты имеют права на выполнение: `chmod +x *.sh`

## 🚨 Устранение проблем

### Если процессы не запускаются:
```bash
# Проверьте логи
pm2 logs --err

# Убейте процессы вручную
pm2 kill
pm2 delete all

# Запустите заново
./start.sh
```

### Если порты заняты:
```bash
# Найдите процесс, занимающий порт
sudo lsof -i :3000
sudo lsof -i :3001

# Убейте процесс
sudo kill -9 <PID>
```

### Если Docker контейнеры не запускаются:
```bash
# Проверьте логи
docker-compose logs

# Пересоздайте контейнеры
docker-compose down
docker-compose up -d --force-recreate
```


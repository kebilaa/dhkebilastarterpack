#!/bin/bash

# Скрипт установки для Digital Hustlas
echo "🚀 Установка Digital Hustlas..."

# Обновляем систему
echo "📦 Обновление системы..."
sudo apt update && sudo apt upgrade -y

# Устанавливаем Node.js (LTS версия)
echo "📦 Установка Node.js..."
curl -fsSL https://deb.nodesource.com/setup_lts.x | sudo -E bash -
sudo apt-get install -y nodejs

# Проверяем версии
echo "✅ Проверка версий:"
node --version
npm --version

# Устанавливаем PM2 для управления процессами
echo "📦 Установка PM2..."
sudo npm install -g pm2

# Устанавливаем зависимости проекта
echo "📦 Установка зависимостей проекта..."
npm install

# Собираем проект
echo "🏗️ Сборка проекта..."
npm run build

# Создаем директории для логов и базы данных
echo "📁 Создание директорий..."
mkdir -p logs
mkdir -p uploads
mkdir -p /var/www/vhosts/194.32.140.220.nip.io/ProdB

# Проверяем наличие базы данных
# ВАЖНО: НЕ перезаписываем базу данных на сервере - она управляется отдельным сервисом
if [ ! -f "/var/www/vhosts/194.32.140.220.nip.io/ProdB/database.db" ]; then
    echo "⚠️  База данных не найдена в /var/www/vhosts/194.32.140.220.nip.io/ProdB/database.db"
    echo "ℹ️  База данных должна быть создана отдельным сервисом (Discord бот)"
    echo "ℹ️  Директория ProdB уже создана выше"
else
    echo "✅ База данных найдена в /var/www/vhosts/194.32.140.220.nip.io/ProdB/database.db"
    echo "ℹ️  База данных НЕ будет перезаписана - она управляется отдельным сервисом"
fi

# Устанавливаем права доступа
echo "🔐 Настройка прав доступа..."
chmod +x start.sh
chmod +x stop.sh
chmod +x restart.sh

echo "✅ Установка завершена!"
echo "📋 Для запуска используйте: ./start.sh"
echo "📋 Для остановки используйте: ./stop.sh"
echo "📋 Для перезапуска используйте: ./restart.sh"
